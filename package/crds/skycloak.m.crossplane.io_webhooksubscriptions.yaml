---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: webhooksubscriptions.skycloak.m.crossplane.io
spec:
  group: skycloak.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - skycloak
    kind: WebhookSubscription
    listKind: WebhookSubscriptionList
    plural: webhooksubscriptions
    singular: webhooksubscription
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'WebhookSubscription is the Schema for the WebhookSubscriptions
          API. A webhook subscription delivering workspace events to an HTTPS endpoint.
          signing_secret and authorization_header are write-only: the API never returns
          them and reads report has_* booleans instead.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebhookSubscriptionSpec defines the desired state of WebhookSubscription
            properties:
              forProvider:
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      only; omit to retain the stored value.
                      Authorization header value sent with each delivery. Write-only; omit to retain the stored value.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clusterId:
                    description: |-
                      (String) Scope deliveries to one cluster. Omit for all clusters.
                      Scope deliveries to one cluster. Omit for all clusters.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether deliveries are active. Defaults to true.
                      Whether deliveries are active. Defaults to `true`.
                    type: boolean
                  eventTypes:
                    description: |-
                      (List of String) Event types to deliver. See the skycloak_webhook_event_types data source for the catalog.
                      Event types to deliver. See the `skycloak_webhook_event_types` data source for the catalog.
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Subscription name.
                      Subscription name.
                    type: string
                  realmId:
                    description: |-
                      (String) Scope deliveries to one Keycloak realm ID (UUID).
                      Scope deliveries to one Keycloak realm ID (UUID).
                    type: string
                  signingSecretSecretRef:
                    description: |-
                      only; never read back.
                      HMAC secret used to sign deliveries, 32 to 512 characters. Write-only; never read back.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  source:
                    description: |-
                      (String) Event source: keycloak or platform.
                      Event source: `keycloak` or `platform`.
                    type: string
                  url:
                    description: |-
                      (String) HTTPS endpoint receiving deliveries.
                      HTTPS endpoint receiving deliveries.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      only; omit to retain the stored value.
                      Authorization header value sent with each delivery. Write-only; omit to retain the stored value.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clusterId:
                    description: |-
                      (String) Scope deliveries to one cluster. Omit for all clusters.
                      Scope deliveries to one cluster. Omit for all clusters.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether deliveries are active. Defaults to true.
                      Whether deliveries are active. Defaults to `true`.
                    type: boolean
                  eventTypes:
                    description: |-
                      (List of String) Event types to deliver. See the skycloak_webhook_event_types data source for the catalog.
                      Event types to deliver. See the `skycloak_webhook_event_types` data source for the catalog.
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Subscription name.
                      Subscription name.
                    type: string
                  realmId:
                    description: |-
                      (String) Scope deliveries to one Keycloak realm ID (UUID).
                      Scope deliveries to one Keycloak realm ID (UUID).
                    type: string
                  signingSecretSecretRef:
                    description: |-
                      only; never read back.
                      HMAC secret used to sign deliveries, 32 to 512 characters. Write-only; never read back.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  source:
                    description: |-
                      (String) Event source: keycloak or platform.
                      Event source: `keycloak` or `platform`.
                    type: string
                  url:
                    description: |-
                      (String) HTTPS endpoint receiving deliveries.
                      HTTPS endpoint receiving deliveries.
                    type: string
                required:
                - signingSecretSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.eventTypes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.eventTypes)
                || (has(self.initProvider) && has(self.initProvider.eventTypes))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.signingSecretSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.signingSecretSecretRef)'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
            - message: spec.forProvider.url is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.url)
                || (has(self.initProvider) && has(self.initProvider.url))'
          status:
            description: WebhookSubscriptionStatus defines the observed state of WebhookSubscription.
            properties:
              atProvider:
                properties:
                  clusterId:
                    description: |-
                      (String) Scope deliveries to one cluster. Omit for all clusters.
                      Scope deliveries to one cluster. Omit for all clusters.
                    type: string
                  createdAt:
                    description: (String)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether deliveries are active. Defaults to true.
                      Whether deliveries are active. Defaults to `true`.
                    type: boolean
                  eventTypes:
                    description: |-
                      (List of String) Event types to deliver. See the skycloak_webhook_event_types data source for the catalog.
                      Event types to deliver. See the `skycloak_webhook_event_types` data source for the catalog.
                    items:
                      type: string
                    type: array
                  hasAuthorizationHeader:
                    description: |-
                      (Boolean) Whether an authorization header is stored.
                      Whether an authorization header is stored.
                    type: boolean
                  hasSigningSecret:
                    description: |-
                      (Boolean) Whether a signing secret is stored.
                      Whether a signing secret is stored.
                    type: boolean
                  id:
                    description: (String) Subscription ID (UUID).
                    type: string
                  name:
                    description: |-
                      (String) Subscription name.
                      Subscription name.
                    type: string
                  realmId:
                    description: |-
                      (String) Scope deliveries to one Keycloak realm ID (UUID).
                      Scope deliveries to one Keycloak realm ID (UUID).
                    type: string
                  source:
                    description: |-
                      (String) Event source: keycloak or platform.
                      Event source: `keycloak` or `platform`.
                    type: string
                  updatedAt:
                    description: (String)
                    type: string
                  url:
                    description: |-
                      (String) HTTPS endpoint receiving deliveries.
                      HTTPS endpoint receiving deliveries.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
