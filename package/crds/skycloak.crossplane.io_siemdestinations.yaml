---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: siemdestinations.skycloak.crossplane.io
spec:
  group: skycloak.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - skycloak
    kind: SIEMDestination
    listKind: SIEMDestinationList
    plural: siemdestinations
    singular: siemdestination
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SIEMDestination is the Schema for the SIEMDestinations API. A
          SIEM forwarding destination for the workspace (syslog, S3, or HTTP). SIEM
          forwarding is plan-gated; the API rejects the calls on plans without it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SIEMDestinationSpec defines the desired state of SIEMDestination
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  batch:
                    description: (Attributes) Batching tuning. Defaults to the API
                      contract values (1000 events, 60 seconds) so an omitted block
                      plans exactly what the server applies. (see below for nested
                      schema)
                    properties:
                      maxEvents:
                        description: |-
                          (Number) Flush after this many events. Defaults to 1000.
                          Flush after this many events. Defaults to `1000`.
                        type: number
                      maxIntervalSeconds:
                        description: |-
                          (Number) Flush at least this often. Defaults to 60.
                          Flush at least this often. Defaults to `60`.
                        type: number
                    type: object
                  enabled:
                    description: |-
                      (Boolean) Whether forwarding is active. Defaults to true.
                      Whether forwarding is active. Defaults to `true`.
                    type: boolean
                  http:
                    description: (Attributes) HTTP destination config (when type is
                      http). (see below for nested schema)
                    properties:
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `basic`, `bearer`, or `none`.
                        type: string
                      bearerTokenSecretRef:
                        description: |-
                          only.
                          Bearer token (when `auth_type` is `bearer`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      headersSecretRef:
                        description: |-
                          only; reads return header names only.
                          Extra HTTP headers. Values are write-only; reads return header names only.
                        properties:
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      passwordSecretRef:
                        description: |-
                          only.
                          Password (when `auth_type` is `basic`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      url:
                        description: |-
                          (String) Collector endpoint URL.
                          Collector endpoint URL.
                        type: string
                      username:
                        description: |-
                          (String) Username (when auth_type is basic).
                          Username (when `auth_type` is `basic`).
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) Destination name.
                      Destination name.
                    type: string
                  s3:
                    description: (Attributes) S3 destination config (when type is
                      s3). (see below for nested schema)
                    properties:
                      accessKeyIdSecretRef:
                        description: |-
                          only.
                          Access key ID (when `auth_type` is `access_key`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `access_key`, `assume_role`, `iam_role`, or `irsa`.
                        type: string
                      bucket:
                        description: |-
                          (String) Bucket name.
                          Bucket name.
                        type: string
                      externalId:
                        description: |-
                          (String) External ID for role assumption.
                          External ID for role assumption.
                        type: string
                      prefix:
                        description: |-
                          (String) Object key prefix.
                          Object key prefix.
                        type: string
                      region:
                        description: |-
                          (String) Bucket region.
                          Bucket region.
                        type: string
                      roleArn:
                        description: |-
                          (String) Role ARN (when auth_type is assume_role).
                          Role ARN (when `auth_type` is `assume_role`).
                        type: string
                      secretAccessKeySecretRef:
                        description: |-
                          only.
                          Secret access key (when `auth_type` is `access_key`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    type: object
                  source:
                    description: (Attributes) Which workspace data stream is forwarded.
                      (see below for nested schema)
                    properties:
                      clusterIds:
                        description: |-
                          (List of String) Cluster IDs to include. Omit for all clusters.
                          Cluster IDs to include. Omit for all clusters.
                        items:
                          type: string
                        type: array
                      keycloakEventTypes:
                        description: |-
                          (List of String) Keycloak event type codes. Required when type is keycloak_events.
                          Keycloak event type codes. Required when `type` is `keycloak_events`.
                        items:
                          type: string
                        type: array
                      realms:
                        description: |-
                          (List of String) Realm names to include. Omit for all realms.
                          Realm names to include. Omit for all realms.
                        items:
                          type: string
                        type: array
                      type:
                        description: |-
                          (String) Destination type: syslog, s3, or http. Immutable.
                          Stream: `application_logs`, `keycloak_events`, `security_logs`, or `skycloak_audit`.
                        type: string
                    type: object
                  syslog:
                    description: (Attributes) Syslog destination config (when type
                      is syslog). (see below for nested schema)
                    properties:
                      format:
                        description: |-
                          (String) rfc5424, cef, leef, or json.
                          `rfc5424`, `cef`, `leef`, or `json`.
                        type: string
                      host:
                        description: |-
                          (String) Syslog server hostname or IP.
                          Syslog server hostname or IP.
                        type: string
                      port:
                        description: |-
                          (Number) Syslog server port.
                          Syslog server port.
                        type: number
                      protocol:
                        description: |-
                          (String) tcp, tls, or udp.
                          `tcp`, `tls`, or `udp`.
                        type: string
                    type: object
                  type:
                    description: |-
                      (String) Destination type: syslog, s3, or http. Immutable.
                      Destination type: `syslog`, `s3`, or `http`. Immutable.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  batch:
                    description: (Attributes) Batching tuning. Defaults to the API
                      contract values (1000 events, 60 seconds) so an omitted block
                      plans exactly what the server applies. (see below for nested
                      schema)
                    properties:
                      maxEvents:
                        description: |-
                          (Number) Flush after this many events. Defaults to 1000.
                          Flush after this many events. Defaults to `1000`.
                        type: number
                      maxIntervalSeconds:
                        description: |-
                          (Number) Flush at least this often. Defaults to 60.
                          Flush at least this often. Defaults to `60`.
                        type: number
                    type: object
                  enabled:
                    description: |-
                      (Boolean) Whether forwarding is active. Defaults to true.
                      Whether forwarding is active. Defaults to `true`.
                    type: boolean
                  http:
                    description: (Attributes) HTTP destination config (when type is
                      http). (see below for nested schema)
                    properties:
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `basic`, `bearer`, or `none`.
                        type: string
                      bearerTokenSecretRef:
                        description: |-
                          only.
                          Bearer token (when `auth_type` is `bearer`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      headersSecretRef:
                        additionalProperties:
                          type: string
                        type: object
                      passwordSecretRef:
                        description: |-
                          only.
                          Password (when `auth_type` is `basic`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      url:
                        description: |-
                          (String) Collector endpoint URL.
                          Collector endpoint URL.
                        type: string
                      username:
                        description: |-
                          (String) Username (when auth_type is basic).
                          Username (when `auth_type` is `basic`).
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) Destination name.
                      Destination name.
                    type: string
                  s3:
                    description: (Attributes) S3 destination config (when type is
                      s3). (see below for nested schema)
                    properties:
                      accessKeyIdSecretRef:
                        description: |-
                          only.
                          Access key ID (when `auth_type` is `access_key`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `access_key`, `assume_role`, `iam_role`, or `irsa`.
                        type: string
                      bucket:
                        description: |-
                          (String) Bucket name.
                          Bucket name.
                        type: string
                      externalId:
                        description: |-
                          (String) External ID for role assumption.
                          External ID for role assumption.
                        type: string
                      prefix:
                        description: |-
                          (String) Object key prefix.
                          Object key prefix.
                        type: string
                      region:
                        description: |-
                          (String) Bucket region.
                          Bucket region.
                        type: string
                      roleArn:
                        description: |-
                          (String) Role ARN (when auth_type is assume_role).
                          Role ARN (when `auth_type` is `assume_role`).
                        type: string
                      secretAccessKeySecretRef:
                        description: |-
                          only.
                          Secret access key (when `auth_type` is `access_key`). Write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    type: object
                  source:
                    description: (Attributes) Which workspace data stream is forwarded.
                      (see below for nested schema)
                    properties:
                      clusterIds:
                        description: |-
                          (List of String) Cluster IDs to include. Omit for all clusters.
                          Cluster IDs to include. Omit for all clusters.
                        items:
                          type: string
                        type: array
                      keycloakEventTypes:
                        description: |-
                          (List of String) Keycloak event type codes. Required when type is keycloak_events.
                          Keycloak event type codes. Required when `type` is `keycloak_events`.
                        items:
                          type: string
                        type: array
                      realms:
                        description: |-
                          (List of String) Realm names to include. Omit for all realms.
                          Realm names to include. Omit for all realms.
                        items:
                          type: string
                        type: array
                      type:
                        description: |-
                          (String) Destination type: syslog, s3, or http. Immutable.
                          Stream: `application_logs`, `keycloak_events`, `security_logs`, or `skycloak_audit`.
                        type: string
                    type: object
                  syslog:
                    description: (Attributes) Syslog destination config (when type
                      is syslog). (see below for nested schema)
                    properties:
                      format:
                        description: |-
                          (String) rfc5424, cef, leef, or json.
                          `rfc5424`, `cef`, `leef`, or `json`.
                        type: string
                      host:
                        description: |-
                          (String) Syslog server hostname or IP.
                          Syslog server hostname or IP.
                        type: string
                      port:
                        description: |-
                          (Number) Syslog server port.
                          Syslog server port.
                        type: number
                      protocol:
                        description: |-
                          (String) tcp, tls, or udp.
                          `tcp`, `tls`, or `udp`.
                        type: string
                    type: object
                  type:
                    description: |-
                      (String) Destination type: syslog, s3, or http. Immutable.
                      Destination type: `syslog`, `s3`, or `http`. Immutable.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: SIEMDestinationStatus defines the observed state of SIEMDestination.
            properties:
              atProvider:
                properties:
                  batch:
                    description: (Attributes) Batching tuning. Defaults to the API
                      contract values (1000 events, 60 seconds) so an omitted block
                      plans exactly what the server applies. (see below for nested
                      schema)
                    properties:
                      maxEvents:
                        description: |-
                          (Number) Flush after this many events. Defaults to 1000.
                          Flush after this many events. Defaults to `1000`.
                        type: number
                      maxIntervalSeconds:
                        description: |-
                          (Number) Flush at least this often. Defaults to 60.
                          Flush at least this often. Defaults to `60`.
                        type: number
                    type: object
                  createdAt:
                    description: (String)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether forwarding is active. Defaults to true.
                      Whether forwarding is active. Defaults to `true`.
                    type: boolean
                  failureCount:
                    description: |-
                      (Number) Consecutive delivery failures.
                      Consecutive delivery failures.
                    type: number
                  healthStatus:
                    description: |-
                      (String) healthy, degraded, or failed.
                      `healthy`, `degraded`, or `failed`.
                    type: string
                  http:
                    description: (Attributes) HTTP destination config (when type is
                      http). (see below for nested schema)
                    properties:
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `basic`, `bearer`, or `none`.
                        type: string
                      hasAuthCredentials:
                        description: |-
                          (Boolean) Whether auth credentials are stored.
                          Whether auth credentials are stored.
                        type: boolean
                      url:
                        description: |-
                          (String) Collector endpoint URL.
                          Collector endpoint URL.
                        type: string
                      username:
                        description: |-
                          (String) Username (when auth_type is basic).
                          Username (when `auth_type` is `basic`).
                        type: string
                    type: object
                  id:
                    description: (String) Destination ID (UUID).
                    type: string
                  lastError:
                    description: |-
                      (String) Most recent delivery error.
                      Most recent delivery error.
                    type: string
                  lastSentAt:
                    description: |-
                      (String) Last successful delivery.
                      Last successful delivery.
                    type: string
                  name:
                    description: |-
                      (String) Destination name.
                      Destination name.
                    type: string
                  s3:
                    description: (Attributes) S3 destination config (when type is
                      s3). (see below for nested schema)
                    properties:
                      authType:
                        description: |-
                          (String) basic, bearer, or none.
                          `access_key`, `assume_role`, `iam_role`, or `irsa`.
                        type: string
                      bucket:
                        description: |-
                          (String) Bucket name.
                          Bucket name.
                        type: string
                      externalId:
                        description: |-
                          (String) External ID for role assumption.
                          External ID for role assumption.
                        type: string
                      hasAccessKeySecret:
                        description: |-
                          (Boolean) Whether a secret access key is stored.
                          Whether a secret access key is stored.
                        type: boolean
                      prefix:
                        description: |-
                          (String) Object key prefix.
                          Object key prefix.
                        type: string
                      region:
                        description: |-
                          (String) Bucket region.
                          Bucket region.
                        type: string
                      roleArn:
                        description: |-
                          (String) Role ARN (when auth_type is assume_role).
                          Role ARN (when `auth_type` is `assume_role`).
                        type: string
                    type: object
                  source:
                    description: (Attributes) Which workspace data stream is forwarded.
                      (see below for nested schema)
                    properties:
                      clusterIds:
                        description: |-
                          (List of String) Cluster IDs to include. Omit for all clusters.
                          Cluster IDs to include. Omit for all clusters.
                        items:
                          type: string
                        type: array
                      keycloakEventTypes:
                        description: |-
                          (List of String) Keycloak event type codes. Required when type is keycloak_events.
                          Keycloak event type codes. Required when `type` is `keycloak_events`.
                        items:
                          type: string
                        type: array
                      realms:
                        description: |-
                          (List of String) Realm names to include. Omit for all realms.
                          Realm names to include. Omit for all realms.
                        items:
                          type: string
                        type: array
                      type:
                        description: |-
                          (String) Destination type: syslog, s3, or http. Immutable.
                          Stream: `application_logs`, `keycloak_events`, `security_logs`, or `skycloak_audit`.
                        type: string
                    type: object
                  syslog:
                    description: (Attributes) Syslog destination config (when type
                      is syslog). (see below for nested schema)
                    properties:
                      format:
                        description: |-
                          (String) rfc5424, cef, leef, or json.
                          `rfc5424`, `cef`, `leef`, or `json`.
                        type: string
                      host:
                        description: |-
                          (String) Syslog server hostname or IP.
                          Syslog server hostname or IP.
                        type: string
                      port:
                        description: |-
                          (Number) Syslog server port.
                          Syslog server port.
                        type: number
                      protocol:
                        description: |-
                          (String) tcp, tls, or udp.
                          `tcp`, `tls`, or `udp`.
                        type: string
                    type: object
                  totalBytesSent:
                    description: (Number)
                    type: number
                  totalEventsSent:
                    description: (Number)
                    type: number
                  totalLogsSent:
                    description: (Number)
                    type: number
                  type:
                    description: |-
                      (String) Destination type: syslog, s3, or http. Immutable.
                      Destination type: `syslog`, `s3`, or `http`. Immutable.
                    type: string
                  updatedAt:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
